[build]
# Static HTML site - uses no-op build script from package.json
command = "npm run build"
publish = "."

# Optional: Add cache headers for better performance
[[headers]]
for = "/*"
  [headers.values]
  Cache-Control = "public, max-age=3600"
