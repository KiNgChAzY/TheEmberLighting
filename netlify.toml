[build]
# No build command needed for static HTML site
# Netlify will simply publish the files
publish = "."

# Optional: Add cache headers for better performance
[[headers]]
for = "/*"
  [headers.values]
  Cache-Control = "public, max-age=3600"
